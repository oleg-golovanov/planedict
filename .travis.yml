language: python

python:
  - 2.7
  - 3.2
  - 3.3
  - 3.4
  - 3.5

install:
  - pip install .
  - pip install coveralls

script:
  - coverage run --source=planedict setup.py test

after_success:
  - coveralls
